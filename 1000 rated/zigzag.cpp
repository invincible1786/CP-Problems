#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;
        vector<int> a(n,-1);
        for(int i=0; i<n; i++) cin >> a[i];
        for(int i=0; i<n-1; i++){
            if(i%2){
                
            }
            else{

            }
        }
    }

    return 0;
}