// link - https://codeforces.com/problemset/problem/1506/C

#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        string a, b;
        cin >> a >> b;
        int n = a.size(), m = b.size();
        int same = 0;
        for(int i=0; i<n; i++){
            for(int j=0; j<m; j++){
                
            }
        }
        cout << m + n - 2 * same << endl;
    }
}